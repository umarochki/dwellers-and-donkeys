export enum AuthRoutes {
    dashboard = '/dashboard',
    newGame = '/new-game',
    tabletop = '/tabletop'
}

export enum NonAuthRoutes {
    login = '/login',
    support = '/support',
    unauthorized = '/unauthorized'
}